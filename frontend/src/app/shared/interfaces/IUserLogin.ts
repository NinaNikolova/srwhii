export interface IUserLogin {
    email: string;
    password: string;
}
// in interfaces we don't need to put any questions mark or exlamation marks or set the default values, because by default any fields inside interface are required;